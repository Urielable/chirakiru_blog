.container
  .post
    %a.post-title{:href => "#permalink"}= @post.title
    .post-date
      por
      %a{:href => "/authors/#{@post.author_id}"}= @post.author.first_name
      en
      = @post.publish_date.pretty
      .post-body
        = raw @post.content
      .post-tags
        - @post.tags_related.each do |tag|
          %a{:href => "/categories/#{tag.id}"}= "##{tag.tag}"
  %hr

  #disqus_thread
    :javascript
      /* * * CONFIGURATION VARIABLES: EDIT BEFORE PASTING INTO YOUR WEBPAGE * * */
      var disqus_shortname = 'chirakirupuroguramingu'; // required: replace example with your forum shortname

      /* * * DON'T EDIT BELOW THIS LINE * * */
      (function() {
          var dsq = document.createElement('script'); dsq.type = 'text/javascript'; dsq.async = true;
          dsq.src = '//' + disqus_shortname + '.disqus.com/embed.js';
          (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
      })();
    %noscript
      Please enable JavaScript to view the
    %a{:href => "http://disqus.com/?ref_noscript"} comments powered by Disqus.